<%= form_with(model: publisher_bookmark, local: true) do |form| %>
  <% if publisher_bookmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publisher_bookmark.errors.count, "error") %> prohibited this publisher_bookmark from being saved:</h2>

      <ul>
      <% publisher_bookmark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :publisher_bookmark_user_id %>
  </div>

  <div class="field">
    <%= form.label :publisher_id %>
    <%= form.text_field :publisher_id, id: :publisher_bookmark_publisher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
